import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  text: {
    fontFamily: 'Montserrat-Regular',
    fontSize: 16,
    color: 'black',
    // Otros estilos globales que desees aplicar a todas las etiquetas Text
  },
});

export default globalStyles;
